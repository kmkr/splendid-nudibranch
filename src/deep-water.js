const DeepWater = ({ onClick }) => (
  <div id="deep-water">
    <div className="link-wrapper">
      <a href="#" onClick={onClick}>
        DROP YOUR WEIGHTS
      </a>
    </div>
  </div>
);

export default DeepWater;
